(function (opakeApp, angular) {
    'use strict';

    opakeApp.factory('Portal', [
        function () {

            var Portal = function (data) {

                angular.extend(this, data);
                var self = this;

		

            };

            return (Portal);
        }]);
})(opakeApp, angular);